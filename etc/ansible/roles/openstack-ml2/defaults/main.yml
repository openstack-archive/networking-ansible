---
# defaults file for openstack-ml2
#

# enable or disable the check for netconf on a junos device.
#
junos_enable_netconf: "{{ junos_enable_netconf }}"

# params to implement port resource
#
port: "{{ port }}"
port_description: "{{ port_description }}"
